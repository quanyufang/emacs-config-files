# Emacs 常用命令参考

> 基于 emacs30-upgrade 配置分支，聚焦文本编辑与笔记管理。

---

## 1. 文件和目录

| 按键 | 命令 | 说明 |
|------|------|------|
| `C-x C-f` | find-file | 打开文件（Vertico 垂直补全） |
| `C-x C-v` | find-alternate-file | 用其他文件替换当前 buffer |
| `C-x i` | insert-file | 在光标处插入文件内容 |
| `C-x C-s` | save-buffer | 保存当前 buffer |
| `C-x C-w` | write-file | 另存为 |
| `C-x C-c` | save-buffers-kill-emacs | 保存并退出 Emacs |
| `C-x d` | dired | 打开目录浏览 |

### Dired 目录操作

| 按键 | 说明 |
|------|------|
| `i` | 在当前 buffer 展开子目录内容 |
| `C-x C-q` | 进入 wdired 模式（直接编辑文件名） |
| `+` | 创建目录 |
| `R` | 重命名文件 |
| `C` | 复制文件 |

---

## 2. 光标移动

| 按键 | 命令 | 说明 |
|------|------|------|
| `C-f` | forward-char | 向前一个字符 |
| `C-b` | backward-char | 向后一个字符 |
| `C-p` | previous-line | 上一行 |
| `C-n` | next-line | 下一行 |
| `M-f` | forward-word | 向前一个词 |
| `M-b` | backward-word | 向后一个词 |
| `C-a` | prelude-move-beginning-of-line | 智能行首（缩进位/行首切换） |
| `C-e` | end-of-line | 行尾 |
| `C-v` | scroll-up | 向下翻页 |
| `M-v` | scroll-down | 向上翻页 |
| `M-<` | beginning-of-buffer | buffer 开头 |
| `M->` | end-of-buffer | buffer 末尾 |
| `C-l` | recenter | 重新居中 |
| `M-g g` | consult-goto-line | 跳转到指定行 |

### Smartparens（结构化移动）

| 按键 | 说明 |
|------|------|
| `C-M-f` | 向前移动到下一个括号 |
| `C-M-b` | 向后移动到上一个括号 |
| `C-M-k` | 删除到括号结尾 |

---

## 3. 文本编辑

### 3.1 删除和剪切

| 按键 | 说明 |
|------|------|
| `DEL` | 删除前一个字符 |
| `C-d` | 删除当前字符 |
| `M-d` | 删除到词尾 |
| `M-DEL` | 删除到词首 |
| `C-k` | 删除到行尾（或删除整行） |
| `C-w` | 剪切选区（无选区时剪切整行） |
| `M-w` | 复制选区（无选区时复制整行） |
| `C-y` | 粘贴（自动缩进） |
| `M-y` | 浏览粘贴历史（consult-yank-from-kill-ring） |

### 3.2 选区操作

| 按键 | 说明 |
|------|------|
| `C-SPC` | 设置标记（选区起点） |
| `C-x h` | 全选 |
| `M-h` | 选中当前段落 |
| `C-x C-x` | 交换光标和标记位置 |
| `M-m` | 按语义逐步扩展选区（expand-region） |
| `C-;` | 多光标编辑（iedit） |

### 3.3 位置变换

| 按键 | 说明 |
|------|------|
| `C-t` | 交换两个字符 |
| `M-t` | 交换两个单词 |
| `C-x C-t` | 交换两行 |

### 3.4 大小写

| 按键 | 说明 |
|------|------|
| `M-c` | 首字母大写 |
| `M-u` | 单词全大写 |
| `M-l` | 单词全小写 |
| `C-x C-u` | 选区全大写 |
| `C-x C-l` | 选区全小写 |

### 3.5 矩形操作（列编辑）

先用 `C-SPC` 设置起点，再移动光标到对角位置：

| 按键 | 说明 |
|------|------|
| `C-x r k` | 剪切矩形块 |
| `C-x r y` | 粘贴矩形块 |
| `C-x r t` | 在所有行前插入相同字符 |

### 3.6 搜索和替换

| 按键 | 说明 |
|------|------|
| `C-s` | 向前增量搜索 |
| `C-r` | 向后增量搜索 |
| `C-c s` | consult-line：当前 buffer 内搜索 |
| `C-c S` | consult-ripgrep：递归全文搜索 |
| `M-%` | 查询替换（anzu-query-replace） |
| `C-M-%` | 正则查询替换 |

### 3.7 其他编辑命令

| 按键 | 说明 |
|------|------|
| `M-/` | hippie-expand：智能补全当前词 |
| `M-;` | 注释/取消注释（comment-dwim-2） |
| `M-c` | 复制当前行（duplicate-thing） |
| `C-o` | 下方插入空行并缩进 |
| `M-o` | 上方插入空行 |
| `C-c i` | 格式化 buffer 或选区 |
| `C-c w` | 显示/隐藏空白字符 |
| `RET` | 换行并自动缩进 |

---

## 4. 撤销与恢复

| 按键 | 说明 |
|------|------|
| `C-g` | 取消当前操作 |
| `C-/` 或 `C-x u` | 撤销 |

---

## 5. Buffer 和窗口

### Buffer 管理

| 按键 | 说明 |
|------|------|
| `C-x b` | 切换 buffer（consult-buffer） |
| `C-x k` | 关闭当前 buffer |
| `C-x C-q` | 切换只读模式 |

### 窗口管理

| 按键 | 说明 |
|------|------|
| `C-x 0` | 关闭当前窗口 |
| `C-x 1` | 智能关闭其他窗口（zygospore，可恢复） |
| `C-x 2` | 水平分割窗口 |
| `C-x 3` | 垂直分割窗口 |
| `C-x o` | 切换到其他窗口 |
| `S-<left>` | 焦点移到左侧窗口 |
| `S-<right>` | 焦点移到右侧窗口 |
| `S-<up>` | 焦点移到上方窗口 |
| `S-<down>` | 焦点移到下方窗口 |

### Frame 管理

| 按键 | 说明 |
|------|------|
| `C-x 5 2` | 新建 Frame |
| `C-x 5 0` | 关闭当前 Frame |
| `C-x 5 o` | 切换到其他 Frame |
| `C-x w f` | 最大化/还原 Frame |

---

## 6. 终端与 Shell

| 按键 | 说明 |
|------|------|
| `C-c t` | 弹出/隐藏终端（shell-pop） |
| `F2` | 打开或切换到 ansi-term（zsh） |
| `M-l` | Eshell 中查看历史命令 |

### Eshell 别名

| 命令 | 等价操作 |
|------|----------|
| `vi file` 或 `vim file` | 用 Emacs 打开文件 |
| `em file` | 同上 |

---

## 7. 笔记管理（C-c n 前缀）

### Denote（文件命名管理）

| 按键 | 功能 |
|------|------|
| `C-c n d` | 新建笔记 |
| `C-c n f` | 查找或创建笔记 |
| `C-c n r` | 重命名笔记文件 |
| `C-c n k` | 添加关键词 |
| `C-c n l` | 创建链接或在链接处创建新笔记 |
| `C-c n b` | 查找反向链接 |

### Org-roam（双向链接）

| 按键 | 功能 |
|------|------|
| `C-c n o` | 查找节点 |
| `C-c n i` | 插入链接 |
| `C-c n t` | 添加标签 |
| `C-c n a` | 添加别名 |
| `C-c n g` | 显示知识图谱 |
| `C-c n s` | 同步数据库 |

### Org-crypt（条目级加密）

| 按键 | 功能 |
|------|------|
| `C-c n c` | 加/解密当前 org 条目 |

> 在 org 条目标题上添加 `:crypt:` 标签，保存时自动加密。适合加密整条笔记。

### Inline-crypt（内联加密，任意文本片段）

| 按键 | 功能 |
|------|------|
| `C-c e` | 加密选中的文本区域 |
| `C-c d` | 解密光标处的加密块 |

> **用法**：选中敏感文本（密码、API key 等）→ `C-c e` → 文本被替换为 GPG 加密块。
> 光标放在加密块上 → `C-c d` → 原地解密查看/编辑（显示为绿色背景）。
> 保存文件时，所有解开的加密块自动重新加密。
>
> 适用于 org、markdown、纯文本所有文件类型。与 org-crypt 互补 —— org-crypt 处理整条笔记，inline-crypt 处理笔记内部的敏感片段。

### Org-capture（快速捕获）

| 按键 | 功能 |
|------|------|
| `C-c c` | 打开捕获菜单 |

---

## 8. 阅读与浏览

| 按键 | 说明 |
|------|------|
| `C-x C-q` | 进入只读模式 |
| `C-x C-+` | 放大字体 |
| `C-x C--` | 缩小字体 |

---

## 9. 编码

| 按键 | 说明 |
|------|------|
| `C-x RET f` | 为当前 buffer 设置编码 |
| `C-x RET r` | 用指定编码重新打开文件 |
| `C-x RET c RET C-x C-w` | 以指定编码另存为 |

---

## 10. 帮助

| 按键 | 说明 |
|------|------|
| `C-h f` | 查看函数文档 |
| `C-h v` | 查看变量文档 |
| `C-h k` | 查看快捷键绑定的命令 |
| `C-h h m` | 显示当前 major-mode 的帮助（discover-my-major） |
| `C-h C-f` | 搜索函数和变量（consult-apropos） |
| `C-h r` | 浏览 Emacs 手册（consult-info） |
| `C-h B` | 显示当前上下文可用的快捷键（embark-bindings） |
| `C-.` | 对当前对象执行操作（embark-act） |

---

## 11. Vertico 补全操作

在 minibuffer 补全列表中：

| 按键 | 说明 |
|------|------|
| `C-n` / `C-p` | 上下选择 |
| `M-<` / `M->` | 跳到开头/结尾 |
| `RET` | 确认选择 |
| `TAB` | 补全当前输入 |
| `C-.' | embark 操作菜单 |

> 搜索时输入多个关键词（用空格分隔），Orderless 会自动模糊匹配。
